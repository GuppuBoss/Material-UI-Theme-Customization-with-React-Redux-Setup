import { fade, makeStyles } from '@material-ui/core/styles';
export default makeStyles((theme) => ({
  customListItem: {
    display: 'flex',
    flexGrow: 1,
    alignItems: 'center'
  },
  mr5: {
    marginRight: 5
  }
}));